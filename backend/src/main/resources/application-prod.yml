spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciador-hotel-spring
    username: postgres
    password: "1234567"
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      format_sql: true
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
